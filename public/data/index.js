import factor from "./factor.js"
import makeChart from "./makeChart.js"

import {getJson, postJson} from "./fetch.js"

const Data = {
  factor,
  makeChart,
  getJson, postJson
}

export default Data;
